# ZhihuVAPI 使用说明

## 项目简介

ZhihuVAPI 是一个优雅的知乎数据调用 Python 包，可以方便地获取知乎上的各种数据。

## 安装和运行

### 1. 环境要求
- Python 3.6+
- requests 库

### 2. 安装依赖
```bash
pip install requests
```

### 3. 运行测试
```bash
python3 test_zhihu.py
```

## 配置说明

### 基本配置
项目的主要配置文件是 `ZhihuVAPI/config.py`，包含以下重要设置：

1. **使用 Chrome Cookies（推荐）**
   - 设置 `is_use_chrome_cookies = True`
   - 脚本会自动读取 Chrome 浏览器的 cookies

2. **自定义配置**
   - 设置 `is_use_chrome_cookies = False`
   - 在 `headers` 中配置正确的 Cookie 和 Authorization

### 获取知乎登录信息

1. **方法一：使用 Chrome 浏览器**
   - 在 Chrome 中登录知乎
   - 脚本会自动读取 cookies

2. **方法二：手动配置**
   - 登录知乎后访问 https://api.zhihu.com/people/self
   - 获取返回的 JSON 中的 `id` 值作为 hash
   - 在 `config.py` 中设置：
     ```python
     hash = '你的hash值'
     is_use_chrome_cookies = False
     headers = {
         'Cookie': '_xsrf=你的值; z_c0=你的值',
         # ... 其他配置
     }
     ```

## 基本使用示例

### 获取用户信息
```python
import ZhihuVAPI as zhihu

# 获取用户信息
person = zhihu.People('用户名或URL')
print(f"用户名: {person.name}")
print(f"赞同数: {person.voteup_count}")
print(f"粉丝数: {person.followers_count}")
```

### 获取问题信息
```python
# 获取问题
question = zhihu.Question('问题URL或ID')
print(f"问题标题: {question.title}")
print(f"回答数: {question.answer_count}")
```

### 获取答案信息
```python
# 获取答案
answer = zhihu.Answer('答案URL')
print(f"答案摘要: {answer.excerpt}")
print(f"赞同数: {answer.voteup_count}")
```

### 获取文章信息
```python
# 获取文章
article = zhihu.Article('文章URL')
print(f"文章标题: {article.title}")
print(f"赞同数: {article.voteup_count}")
```

### 获取专栏信息
```python
# 获取专栏
column = zhihu.Column('专栏URL或ID')
print(f"专栏标题: {column.title}")
print(f"文章数: {column.articles_count}")
```

## 高级功能

### 获取列表数据
```python
# 获取用户的答案列表
for answer in person.answers(count=5):
    print(f"答案: {answer.excerpt}")

# 获取用户的粉丝列表
for follower in person.followers(count=10):
    print(f"粉丝: {follower.name}")
```

### 交互功能
```python
# 点赞答案
answer.vote()

# 关注用户
person.follow()

# 发送私信
person.send('你好！')
```

## 常见问题

### 1. 错误代码 100
- 原因：Headers 中的 Authorization 配置错误
- 解决：检查并更新 `config.py` 中的 Authorization 值

### 2. 错误代码 10003
- 原因：请求参数异常，需要升级客户端
- 解决：更新 User-Agent 或其他请求头信息

### 3. 错误代码 40362
- 原因：请求被限制访问
- 解决：检查登录状态，可能需要重新登录知乎

### 4. 模块导入错误
- 原因：缺少依赖或路径问题
- 解决：确保已安装 requests 库，并正确设置 Python 路径

## 注意事项

1. **遵守知乎规则**：使用 API 时请遵守知乎的使用条款
2. **频率限制**：避免过于频繁的请求，以免被限制
3. **隐私保护**：不要获取或使用他人的隐私信息
4. **数据使用**：获取的数据仅用于学习和研究目的

## 技术支持

如果遇到问题，可以：
1. 检查配置文件是否正确
2. 确认知乎登录状态
3. 查看错误日志信息
4. 参考项目文档和示例

---

**项目已成功运行！** 🎉

现在您可以开始使用 ZhihuVAPI 来优雅地调用知乎数据了。